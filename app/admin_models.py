# admin_models.py — kept as placeholder after WebAuthn removal in v4.8.0.
# The AdminUser, WebAuthnCredential, and WebAuthnChallenge models have been
# removed. Admin access is now controlled by User.is_admin + the
# admin_or_local decorator in auth_utils.py.
